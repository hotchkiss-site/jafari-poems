__pycache__/
*.pyc
*.pyo
.DS_Store
venv/
*.egg-info/
dist/
build/
.env

# old build output (superseded by index.html)
collection.html

# scratch / backup files
*- Copy.py

# raw source photographs of the book pages — bulky, and the whole point of
# "raw photos/persian_poems.md" is that the text has been lifted out of them.
# Keep the markdown (and any future FLAGS/notes), drop the images.
raw photos/*
!raw photos/*.md
